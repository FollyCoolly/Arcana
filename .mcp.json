{
  "mcpServers": {
    "arcana": {
      "type": "stdio",
      "command": "cargo",
      "args": [
        "run",
        "--manifest-path",
        "src-tauri/Cargo.toml",
        "--bin",
        "mcp-server"
      ]
    }
  }
}
